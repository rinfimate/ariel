# Generated by uniffi-bindgen-react-native
[package]
name = "react-native-ariel"
version = "0.3.0"
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []

[dependencies]
mermaid_wrapper = { path = "../../rust/mermaid_wrapper" }
# Use the runtime crate that ships with the current UBRN install so the
# generated wasm crate stays in lock-step with the CLI/runtime implementation.
uniffi-runtime-javascript = { path = "../../node_modules/uniffi-bindgen-react-native/crates/uniffi-runtime-javascript", features = ["wasm32"] }
wasm-bindgen = "*"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"

[workspace]
